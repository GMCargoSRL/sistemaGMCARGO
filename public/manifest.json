import withPWA from 'next-pwa';

const nextConfig = withPWA({
  dest: 'public',
  register: true,
  skipWaiting: true,
  disable: process.env.NODE_ENV === 'development', // Solo actúa al compilar para producción
})({
  // Tus configuraciones previas si las tienes
});

export default nextConfig;